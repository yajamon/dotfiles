#compdef dg-switch

# 定数 (readonlyをつけたら動かなくなるぞ！！！)
DG_ROOT="$HOME/.dg"
SEP='.'

_dg-switch() {
    _arguments \
        '(- *)-h[show help]' \
        '(- *)-l[list accounts]' \
        '*:accounts:__dg-switch'
}

__dg-switch() {
    _values 'accounts' $(cd $DG_ROOT && dg-switch -l )
}
